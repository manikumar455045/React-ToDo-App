import React from 'react';

const Form = () => {
  return(
    <div>
      <form>
        <input type="text" placeholder="enter a Backlog" />
        <button type="submit">Add</button>

      </form>
    </div>
  )
}
export default Form;